<div class="container">
<% @users.each do |user| %>
  <div class="user-wrapper">
    <img src="<%=user.photo%>" />
    <div class="content">
      <h2><%= link_to user.name, user_path(user) %></h2>
      <p>Number of posts: <%=user.posts_counter%></p>
    </div>
  </div>
<%end %>
</div>
